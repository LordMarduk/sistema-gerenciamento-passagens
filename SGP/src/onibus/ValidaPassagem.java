/* ValidaPassagem - Recebe o codigo da passagem e verifica no bd
 * em passagem->passagem_pk se o codigo bate e se o mesmo tem a coluna do BD
 * passagemValida=true, ou seja, if(numPassagem == passagem_pk && passagemValida==true)
 * se sim retorna true para main, caso contrario false.
 * Obs.: Se a pessagem for válida, antes de retorna true para main, ele seta
 * passagemValida como false, para evitar que ela seja usada outra vez.
 */

package onibus;

class ValidaPassagem {

    public ValidaPassagem(){


    }

}
